# How to use

```yaml
- name: Publish npm snapshots
  id: snapshot
  uses: fluencelabs/github-actions/npm-publish-snapshot@main
  with:
    id: sfs832s-master-1-2 # snapshot id
    working-directory: npm-package/
```
